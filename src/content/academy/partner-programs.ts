import "server-only";
import { definePartnerPrograms } from "@/lib/academy-partner-programs";

// Site-owned, seeded once. Empty means no additional Academy UI or requirements.
export const partnerPrograms = definePartnerPrograms([]);
